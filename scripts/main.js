document.addEventListener('DOMContentLoaded', () => {
  // Initialize scroll-based animations
  new ScrollAnimations();

  // Initialize demonstration section animations
  new DemonstrationAnimation();
});
