# CLAUDE

Canonical repo guidance lives in [Agent.md](./Agent.md).

Use that file for mission, stack, commands, and development rules. Keep this shim minimal so tooling that expects `CLAUDE.md` can still resolve the project guide.

